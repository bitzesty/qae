dependencies:
  bundler:
    without: [development, deployment]
database:
  pre:
    - cp .env.example .env
test:
  override:
    - bundle exec rspec spec/ --tag '~skip_ci' --backtrace spec --format progress
