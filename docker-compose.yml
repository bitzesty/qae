web: &web
  build: .
  command: bundle exec rails server -b 0.0.0.0 -P /tmp/server.pid
  ports:
    - '80:3000'
    - '443:3000'
  volumes:
    - .:/app
worker:
  <<: *web
  ports: []
  command: bundle exec sidekiq
