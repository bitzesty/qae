input.js-trigger-autosave class="#{question.style.present? ? question.style : 'medium'}" type="text" min=question.min max=question.max name=question.key value=answers[question.key] *possible_read_only_ops
-if question.unit
  = question.unit
