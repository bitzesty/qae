.question-group
  = country_select(question, "#{question.key}_country", {priority_countries: ["GB"], selected: answers["#{question.key}_country"]}, possible_read_only_ops.merge({name: "#{question.key}_country"}))