input class="govuk-input js-trigger-autosave #{question.style.present? ? question.style : 'govuk-input--width-10'}" type="#{question.type.present? ? question.type.to_s : 'text'}" name=question.input_name value=question.input_value autocomplete="off" id="q_#{question.key}" aria-label="#{question.title}" *possible_read_only_ops
