<% if collaborator.persisted? %>
  $.fancybox.close();
  $(".js-collaborators-list").append("<%= j(render 'item', collaborator: collaborator.decorate) %>");
<% else %>
  $.fancybox("<%= j(render 'form') %>");
<% end %>