.comment
  - if comment.author?(current_admin)
    = form_for(comment, url: admin_form_answer_comment_path(id: comment.id, form_answer_id: resource.id), html: { method: :delete }) do |f|
      / TODO confirm alert to delete comment
      = f.submit 'Delete', class: "comment-delete"
  .comment-header
    h3
      = comment.author_email
      small = l comment.created_at, format: :date_at_time
  .comment-content
    = comment.body

