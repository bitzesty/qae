.panel.panel-default
  .panel-heading#appraisal-form-moderated-heading role="tab"
    h4.panel-title
      a data-toggle="collapse" data-parent="#panel-assessment" href="#section-appraisal-form-moderated" aria-expanded="true" aria-controls="section-appraisal-form-moderated"
        ' Appraisal Form (Moderated)
        small
          = primary_assessment.last_editor_info

  #section-appraisal-form-moderated.section-appraisal-form.section-appraisal-form-moderated.panel-collapse.collapse.in role="tabpanel" aria-labelledby="appraisal-form-moderated-heading"
    .panel-body
      = simple_form_for([namespace_name, moderated_assessment], remote: true, authenticity_token: true) do |f|
        = f.hidden_field :form_answer_id
        = render_section(resource, f)
        = f.submit :add, class: "if-js-hide"
      - unless moderated_assessment.submitted?
        = form_tag(url_for([namespace_name, :assessment_submissions]), remote: true, class: "submit-assessment", "data-type" => "json")
          = hidden_field_tag :assessment_id, moderated_assessment.id
          = submit_tag "Submit Case Assessment"
          .feedbackHolder
      - else
        span Assessment Submitted
