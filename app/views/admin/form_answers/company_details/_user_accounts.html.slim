.form-group
  .form-container
    label.form-label User accounts

    - if (resource.account.try(:users) || []).any?
      ul.list-unstyled.list-actions
        - (resource.account.try(:users) || []).each do |user|
          li
            = link_to "#{user.full_name} (#{user.email})", edit_admin_user_path(user)
    - else
      p
        em.text-muted No users were added.
